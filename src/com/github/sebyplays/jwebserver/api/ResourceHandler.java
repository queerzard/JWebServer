package com.github.sebyplays.jwebserver.api;

public class ResourceHandler {
    //TODO Scan for resources.
    //TODO if a request is made to a context that isn't staticly registered, then the files within the application's
    // folder are scanned for resources with the same path/name and then sent to add support for images, html & css files etc.
}
